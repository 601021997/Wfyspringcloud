spring:
  application:
    name: discovery-service-eurekaServer1
---
spring:
  profiles: peer1
server:
  port: 8761
eureka:
  instance:
    hostname: peer1
  client:
    service-url:
      defaultZone: http://peer2:8762/eureka


#server:
#  port: 8761
# eureka:
 # client:
  #  register-with-eureka: false
   # fetch-registry: false
   # service-url:
   #  defaultZone: http://localhost:8761/eureka
 #management:
  #endpoints:
   # jmx:
    #  exposure:
     #   include: health,info
#management:
 # endpoints:
 #   enabled-by-default: true
  #endpoint:
  #  info:
  #    enabled: true


